// Compulsory: Name of the component must be capitalise
// both in react and vite
// without this component couldn't render on browser

// for good practice: file name of components should be capitalise
// without this also code runs but not a good practice

// if only javascript is return then filename.js
// if html is return then filename.jsx
function Chai(){
    return(
        <h2>Chai in react</h2>
    )
}

export default Chai